## Packages
framer-motion | Page transitions and smooth micro-interactions
date-fns | Human-readable date formatting

## Notes
JWT Authentication token should be stored in localStorage as 'auth_token'
API requests to /api/users and /api/tasks require Authorization: Bearer <token> header
Tailwind custom fonts configured via CSS variables
